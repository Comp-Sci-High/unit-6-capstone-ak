<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Teachers</title>
  <link rel="stylesheet" href="/script.css" />
  <script src="/teacherScript.js" defer></script>
</head>
<body>
  <nav class="navbar">
    <div class="navbar-logo">
      <img src="https://images.squarespace-cdn.com/content/v1/5b328976aa49a1b7b46b89c3/1530039440176-O24B7OCR4WPIAI62JZTW/CompSci.High_Circle_Logo.png?format=750w" alt="School Logo" />
    </div>
    <div class="nav-links">
      <a href="/">Teachers</a>
      <a href="/ratings">Comments & Ratings</a>
    </div>
  </nav>

  <div class="container">
    <h1>Apps</h1>

    <div class="form-section">
      <h2>Add a New APP</h2>
      <form>
        <label>commer: </label>
        <input type="text" name="name" required />

        <label>Rating: </label>
        <input type="text" name="department" />

        <label>pfpURL: </label>
        <input type="text" name="image" />

        <button type="submit">Submit</button>
      </form>
    </div>

    <div class="Apps-grid">
      <% for (let i = 0; i < app.length; i++) { %>
        <div class="app-card">
          <img src="<%= app[i].image %>" alt="<%= app[i].comment %>" />
          <div class="app-card-content">
            <h2><%= app[i].name %></h2>
            <p><strong>Rating:</strong> <%= app[i].rating %></p>
            <!-- Add a button that calls a deleteTeacher function with their id passed as input -->
             <button onclick="deleteApp('<%= app[i]._id %>')">Delete</button>

          </div>
        </div>
      <% } %>
    </div>

  </div>
</body>
</html>

